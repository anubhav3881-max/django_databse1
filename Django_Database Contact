Contact form se user entry karta hai data ki!

Contact.html

{% load static %}
<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="{% static 'css/bs.css' %}">
</head>

<body class="bg bg-light">


    <div class=" form container bg bg-white my-5 py-5 px-5">
        <h1 class="text text-danger text-center">Register Now</h1>
        <div class="d-flex">
            
            {% if user.is_authenticated %}
            <a href="../logout" class="btn btn-danger">Logout</a>
            {% else %}
            <a href="../signup" class="btn btn-primary me-auto">SignUp</a>
            <a href="../login" class="btn btn-secondary">Login</a>
            {% endif %}
        </div>
        <form method="POST" class="shadow py-4 px-4" enctype="multipart/form-data">
            {% csrf_token %}
            <div class="form-group my-4">
                <input type="text" name="name" class="form-control" value="{{data.name}}" placeholder="Type your name">
            </div>
            <div class="form-group my-4">
                <input type="email" name="email" class="form-control" value="{{data.email}}" placeholder="Type your Email">
            </div>
            <div class="form-group my-4">
                <input type="number" name="contact" class="form-control" value="{{data.contact}}" placeholder="Type your Contact">
            </div>
            <select name="city" class="form-group form-control my-4" required>
                <option selected disabled>--Select a city--</option>
                <option value="Kanpur">Kanpur</option>
                <option value="Jaipur">Jaipur</option>
                <option value="Lucknow">Lucknow</option>
                <!-- Add more city options as needed -->
            </select>
            <div class="form-group my-4">
                <input type="file" name="image" class="form-control">
            </div>
            <br>
            <input type="hidden" name="userid" value="{{data.id}}">
            <button type="submit" class="btn btn-primary">Submit</button>
            <a href="../display" class="btn btn-primary ms-auto">Display</a>
             <p>{{success}}</p>
        </form>
       
    </div>

</body>
<script src="{% static 'js/bs.js' %}"></script>

</html>
